import time
from activemonitors import ActiveMonitors

class Timer:

    def __init__(self):
        self.minute = 0

    def startTimer(self):
        pass

    #unsure if we'll if we're going to keep the timer going
    def resetTimer(self):
        self.minute = 0
